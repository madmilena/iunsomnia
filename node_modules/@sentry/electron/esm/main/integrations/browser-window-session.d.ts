export interface Options {
    /**
     * Number of seconds to wait before ending a session after the app loses focus.
     *
     * Default: 10 seconds
     */
    backgroundTimeoutSeconds?: number;
}
/**
 * Tracks sessions as BrowserWindows focus.
 *
 * Supports Electron >= v12
 */
export declare const browserWindowSessionIntegration: (options?: Options | undefined) => import("@sentry/core").Integration;
//# sourceMappingURL=browser-window-session.d.ts.map