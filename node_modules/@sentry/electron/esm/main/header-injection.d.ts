import { Session } from 'electron';
/**
 * Adds a header to a session's web request
 */
export declare function addHeaderToSession(sesh: Session, header: string, value: string): void;
//# sourceMappingURL=header-injection.d.ts.map