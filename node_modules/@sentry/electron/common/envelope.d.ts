import { Attachment, Envelope, Event, Profile } from '@sentry/core';
/** Pulls an event and additional envelope items out of an envelope. Returns undefined if there was no event */
export declare function eventFromEnvelope(envelope: Envelope): [Event, Attachment[], Profile | undefined] | undefined;
//# sourceMappingURL=envelope.d.ts.map