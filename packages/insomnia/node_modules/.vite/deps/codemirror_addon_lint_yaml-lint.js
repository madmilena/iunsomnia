import {
  require_codemirror
} from "./chunk-6ISSAUZX.js";
import {
  __commonJS
} from "./chunk-PLDDJCW6.js";

// ../../node_modules/codemirror/addon/lint/yaml-lint.js
var require_yaml_lint = __commonJS({
  "../../node_modules/codemirror/addon/lint/yaml-lint.js"(exports, module) {
    (function(mod) {
      if (typeof exports == "object" && typeof module == "object")
        mod(require_codemirror());
      else if (typeof define == "function" && define.amd)
        define(["../../lib/codemirror"], mod);
      else
        mod(CodeMirror);
    })(function(CodeMirror2) {
      "use strict";
      CodeMirror2.registerHelper("lint", "yaml", function(text) {
        var found = [];
        if (!window.jsyaml) {
          if (window.console) {
            window.console.error("Error: window.jsyaml not defined, CodeMirror YAML linting cannot run.");
          }
          return found;
        }
        try {
          jsyaml.loadAll(text);
        } catch (e) {
          var loc = e.mark, from = loc ? CodeMirror2.Pos(loc.line, loc.column) : CodeMirror2.Pos(0, 0), to = from;
          found.push({ from, to, message: e.message });
        }
        return found;
      });
    });
  }
});
export default require_yaml_lint();
//# sourceMappingURL=codemirror_addon_lint_yaml-lint.js.map
